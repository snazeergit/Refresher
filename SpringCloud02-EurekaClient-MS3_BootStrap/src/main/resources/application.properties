server.port=9093
spring.application.name=MS3
eureka.client.service-url.default-zone=http://localhost:8761/eureka

#Approach 1:Connecting Config server that runs on other than default port no
#spring.config.import=optional:configserver:http://localhost:8811

# Approach 2:Connecting Config server that runs on other than default port no
# Create a bootstrap.properties file in src/main/resources with the below entry 
# spring.cloud.config.uri=http://localhost:8811